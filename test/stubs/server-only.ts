// Test stub for the `server-only` package. In a Next build it's a compile-time
// guard that errors if a server module is pulled into a client bundle; under
// vitest there is no bundler boundary, so it's a harmless no-op.
export {};
